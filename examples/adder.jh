bool x = true;
bool y = false;
print(!x || (x && !y));

module foo(input a, input b, output c) {
    num temp = a | b;

    c = temp + 2;
}

num b = 5;
num temp = 0;
num max = 0;

foo(2, b, max);
while (temp < max) {
    print(temp);
    temp = temp + 1;
}